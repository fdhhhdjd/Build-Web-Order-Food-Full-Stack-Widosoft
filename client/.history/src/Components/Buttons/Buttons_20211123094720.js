import React from 'react'

const Button = ({class,name}) => {
    return (
        <>
        <Button>
        <a href="" className={class}>{name}</a>
        </Button>
        </>
    )
}

export default Button
